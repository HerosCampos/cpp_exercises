#include <iostream>
#include <vector>

int main()
{
    // std::cout << "Enter an integer: ";
    // int num{};
    // std::cin >> num;

    // std::cout << num << " is " << ((num % 2 == 0) ? "even" : "odd") << std::endl;

    int num1{50}, num2{75};

    std::cout << num1 << " is " << ((num1 > num2) ? "bigger" : "smaller") << " than " << num2 << std::endl;

}
